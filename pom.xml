<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>cn.structured</groupId>
        <artifactId>structure-dependencies</artifactId>
        <version>1.4.0</version>
    </parent>


    <name>structure-pro-infra</name>
    <artifactId>structure-pro-infra</artifactId>
    <version>${revision}</version>
    <packaging>pom</packaging>
    <description>pro项目父工程</description>

    <properties>
        <revision>1.0.0-SNAPSHOT</revision>
        <spring-boot.version>4.0.6</spring-boot.version>
        <mybatis-plus.version>3.5.16</mybatis-plus.version>
        <structure.version>1.4.3-SNAPSHOT</structure.version>
        <structure-security.version>1.1.4</structure-security.version>
        <structure-datascope.version>1.0.3</structure-datascope.version>
    </properties>

    <modules>
        <module>structure-infra-starter</module>
        <module>structure-infra-mybatis-plus-starter</module>
        <module>structure-infra-jpa-starter</module>
        <module>structure-infra-mongodb-starter</module>
        <module>structure-infra-elasticsearch-starter</module>
        <module>structure-infra-sample</module>
    </modules>

    <dependencyManagement>
        <dependencies>

            <!-- 内部公共组件 -->
            <dependency>
                <groupId>cn.structured</groupId>
                <artifactId>structure-common</artifactId>
                <version>${structure.version}</version>
            </dependency>
            <!-- mybatis-plus -->
            <dependency>
                <groupId>cn.structured</groupId>
                <artifactId>structure-mybatis-plus-starter</artifactId>
                <version>${structure.version}</version>
            </dependency>
            <!-- web -->
            <dependency>
                <groupId>cn.structured</groupId>
                <artifactId>structure-restful-web-starter</artifactId>
                <version>${structure.version}</version>
            </dependency>
            <!-- 租户 -->
            <dependency>
                <groupId>cn.structured</groupId>
                <artifactId>structure-tenant-starter</artifactId>
                <version>${structure.version}</version>
            </dependency>

            <!-- MyBatis Plus -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot4-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <!-- mybatis-plus-jsqlparser -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <!-- SpringDoc OpenAPI -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- 安全框架 -->
            <dependency>
                <groupId>cn.structured</groupId>
                <artifactId>structure-security-core</artifactId>
                <version>${structure-security.version}</version>
            </dependency>
            <!-- 权限 -->
            <dependency>
                <groupId>cn.structured</groupId>
                <artifactId>structure-security-permission-starter</artifactId>
                <version>${structure-security.version}</version>
            </dependency>

            <!-- 数据权限 -->
            <dependency>
                <groupId>cn.structured</groupId>
                <artifactId>structure-datascope-starter</artifactId>
                <version>${structure-datascope.version}</version>
            </dependency>


            <dependency>
                <groupId>cn.structured</groupId>
                <artifactId>structure-datascope-message</artifactId>
                <version>${structure-datascope.version}</version>
            </dependency>

            <dependency>
                <groupId>cn.structured</groupId>
                <artifactId>structure-datascope-cache</artifactId>
                <version>${structure-datascope.version}</version>
            </dependency>

            <!-- 数据权限-mybatis-plus -->
            <dependency>
                <groupId>cn.structured</groupId>
                <artifactId>structure-datascope-mybatis-plus-starter</artifactId>
                <version>${structure-datascope.version}</version>
            </dependency>

            <!-- JPA API 版本覆盖，支持 Spring Data JPA 3.3+ 需要的 FindOption -->
            <dependency>
                <groupId>jakarta.persistence</groupId>
                <artifactId>jakarta.persistence-api</artifactId>
                <version>3.1.0</version>
            </dependency>

            <dependency>
                <groupId>cn.structured</groupId>
                <artifactId>structure-infra-starter</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>cn.structured</groupId>
                <artifactId>structure-infra-mybatis-plus-starter</artifactId>
                <version>${revision}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

</project>